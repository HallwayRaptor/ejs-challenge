<%- include("partials/header") %>  
<h1>Home</h1>
<p> <%= homeStartingContent %> </p>
<%  posts.forEach(post => {  %>
  <!-- render post on home page -->
    <h1><%= post.title %></h1>
    <p><%= post.content %></p>
<% }); %>

<%- include("partials/footer") %> 